import { AnimationDefinition } from '../types';

const data: AnimationDefinition = {
    name: '...',
    componentLoader: () => import('./....vue'),
    summary: '...',
    category: 'other',
};

export default data;
